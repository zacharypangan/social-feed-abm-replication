# Safe placeholder environment file.
# Copy to `.env` for local use and replace values with real local secrets.
# Never commit real secrets.

PROJECT_NAME=social-feed-abm-replication
ENVIRONMENT=development
LOG_LEVEL=INFO

# Example API settings. Leave blank until the duplicated project needs them.
API_BASE_URL=http://localhost:8000
OPENAI_API_KEY=

# Example data paths.
DATA_DIR=data
OUTPUT_DIR=outputs
